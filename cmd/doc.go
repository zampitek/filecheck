// Package cmd is responsible for handling the CLI through cobra-cli.
//
// It contains a root.go file which represents the root command (filecheck),
// as well as other files for each subcommand, that are associated to a commmand
// higher in hierarchy or to the root command directly.
package cmd
